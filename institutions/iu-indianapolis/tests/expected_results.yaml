fixtures:
  baseline.pdf:
    assert_fails: []
    assert_passes:
      - global_margins
      - margin_symmetry
      - font_size_consistent
      - font_family_consistent
      - title_page_no_bold
      - title_page_no_page_number
      - page_numbers_format
      - footnotes_font_consistent
      - new_chapters_new_pages
      - hyperlinks_format
      - front_matter_order
    ignore_others: true

  golden.pdf:
    assert_fails: []
    assert_passes:
      - margin_symmetry
      - font_size_consistent
      - font_family_consistent
      - justification_consistent
      - front_matter_order
      - title_page_no_bold
      - copyright_page_format
      - headings_consistent
      - footnotes_font_consistent
      - new_chapters_new_pages
      - hyperlinks_format
      - cv_name_position
    ignore_others: true

  left-narrow.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  right-narrow.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  left-wide.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  right-wide.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  top-narrow.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  bottom-narrow.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  top-wide.pdf:
    assert_fails: ["global_margins"]
    assert_passes: []
    ignore_others: true

  asymmetric.pdf:
    assert_fails: ["margin_symmetry"]
    assert_passes: []
    ignore_others: true

  messy.pdf:
    assert_fails: []
    assert_passes: []
    ignore_others: true
